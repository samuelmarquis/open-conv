[confirmed] 1. Dynamic convolution selects/interpolates among M pre-measured, amplitude-normalized IRs by the instantaneous magnitude of each individual delayed input sample x(n-k) at each tap k (not a single global level estimate), so different taps within one convolution sum can pull from different IR pairs — per the quoted equation y(n) = Σx(n-k).(p(x(n)).h_S(x(n-k))(k) + (1-p(x(n))).h_(S(x(n-k))-1)(k)).
  evidence: Independently re-downloaded the cited Sintefex PDF via a separate route (direct curl of the wayback-machine copy, bypassing the failed WebFetch/archive.org path) and read all 15 pages directly. Equation (2)/(3) on p.5-6 of 'Dynamic Convolution' (Kemp, Sintefex, 1999/2000) match the quote verbatim, including the selector S(x(n-k)) being evaluated per-tap on the delayed sample. The general 'per-input-sample dynamic IR selection' concept is independently corroborated by Jason Traub, 'Vacuum Tube Amplifier Modeling with Dynamic Convolution,' Univ. of Florida Journal of Undergraduate Research 17(2), Spring 2016, which cites the identical Kemp/AES-106 title and describes 'choosing an appropriate impulse response for each input sample.' Note: the primary equation itself uses p(x(n)) (fixed at n) rather than p(x(n-k)) even though S is per-tap — this looks like an internal inconsistency/typo in Kemp's own paper, but it is reproduced faithfully in the quote, so the quote is accurate to the source.
  CORRECTION: None needed for substance; minor note: Kemp's own equation (3) mixes S(x(n-k)) (per-tap) with p(x(n)) (not per-tap) — an apparent internal inconsistency in the original document, not an error introduced by the claim.

[confirmed] 2. Selector is a floor-division quantizer of |x(n)| into M linear bins of width fs/M, with linear interpolation via fraction p(x(n)) using modulo/floor definitions as quoted.
  evidence: Directly verified against an independently re-fetched full copy of the primary Sintefex 'Dynamic Convolution' document (p.5-6): 'S(x(n)) = 1 + {|x(n)| / (fs / M)}' and 'p(x(n)) = (|x(n)| modulo (fs / M)) / (fs / M)' appear verbatim, with the braces explicitly defined as 'the integral part of the value' and the text explicitly states linear (not spline/cubic) interpolation was chosen ('we have chosen a simple linear interpolation... an arbitrary complex interpolator could be designed but...').

[confirmed] 3. 128 levels chosen because devices are linear >40dB below peak; IRs derived from step (not unit) impulses via first-order differencing.
  evidence: Verbatim match in the independently re-fetched primary document (p.3-4): '...128 linearly spaced impulse levels were chosen...' with the 40dB linear-region rationale, and 'These are converted to "unit" impulse response by first order differing of the derived signal, yn=xn-xn-1,' with separate rationale earlier that 'the step has significantly more energy in the lower frequency than the unit impulse.' Independently corroborated in substance by Sound On Sound's review of the Sintefex FX2000/CX2000 (soundonsound.com), which states Sintefex 'employ impulses at 127 different levels' — same order of magnitude/rationale, off by one from the paper's 128, likely a reviewer rounding/off-by-one (paper samples 128 impulses from max down to non-inclusive zero) rather than a substantive contradiction.
  CORRECTION: Minor: an independent, reputable secondary source (Sound On Sound) states '127' rather than '128' levels for the shipped product; this is very likely off-by-one imprecision in the review rather than a real discrepancy, since the primary paper is unambiguous and internally consistent about 128.

[confirmed] 4. Noise remediation via splicing higher-level IR data at points nearing the noise floor, cross-fading at splice boundaries, cascading down through all 128 levels.
  evidence: Verbatim in the independently re-fetched primary document (p.8-9, Section 4): '...matching sections from the next higher level signal can be substituted (with appropriate gradual cross fading at either end of the "splice").' and 'The process is repeated down to the lowest level impulse.' Quote is accurate and the cascading-through-all-levels description is directly supported.

[confirmed] 5. FX8000-class hardware: 2-MAC/2-fetch inner loop, 2 cycles/tap, 60MHz ADSP-21065L SHARC, 512 taps in ~17µs (50kHz, 15% margin), cascaded 2 rows of 4 SHARCs + 1 coordinator for stereo at 2048 taps/channel, 50kHz target (100kHz supported with reduced IR length), 24-bit internal precision.
  evidence: Verbatim/near-verbatim match in the independently re-fetched primary document (Section 6, p.10): exact quoted sentences appear, plus the design criteria list confirms '2048 taps per channel... up to 50kHz,' '100kHz... with loss of impulse duration,' and '24-bit internal precision.' Cross-checked arithmetic independently: 512 taps × 2 cycles / 60MHz = 17.07µs, and 17/20µs (1/50kHz) = 85% duty → 15% margin, both self-consistent. The SHARC 'two memory accesses + one MAC per clock' claim is independently confirmed by general ADSP-2106x/SHARC architecture documentation (Super Harvard Architecture descriptions on Wikipedia/ADI docs). The ADSP-21065L's 60MHz speed grade is independently confirmed via Analog Devices' own datasheet and a Digi-Key mirror ('Two speed grades... 60 MHz and 66 MHz instruction rates'). The total chip count is independently corroborated: Sound On Sound's review states the related FX2000 uses '10 SHARC chips' (1 admin + 9 convolution) and other reviews (Gearspace/Reverb) state the flagship FX8000 uses 'up to 37 SHARC DSP chips' — consistent with 4 stereo sections × 9 chips (8 convolution + 1 coordinator, per the paper) = 36, plus one further system-level chip ≈ 37.

[confirmed] 6. Selector generalizes to (a) envelope/history-based S(x,n), (b) sign-dependent selection via separate +/- step-impulse IR sets, (c) a second dimension (user param/LFO/compressor gain reduction) combined via bilinear interpolation.
  evidence: All three sub-claims verified verbatim against the independently re-fetched primary document (p.6-9, Sections 2 and 5): 'This can easily be generalised as S(x,n)... We have done some tests using an envelope of the input signal...'; 'It is however possible to use impulse response samples taken with positive going and negative going impulses and to use the sign of the incoming audio samples...'; 'Bilinear interpolation is now used to select in one dimension on amplitude and on another dimension by any desirable method. One example is a user controllable parameter... another is an oscillator controlled parameter... A further useful parameter may be gain reduction in a compressor simulation.' Independently corroborated by a separate, later AES convention paper by the same author — Kemp, 'Analysis and Simulation of Analogue Dynamic Compressors and Limiters in the Digital Domain,' AES 109th Convention, Sept 2000, preprint #5185 — which per secondary description implements exactly the compressor-gain-reduction-as-second-dimension extension foreshadowed here, confirming this was a real, realized generalization and not merely speculative text.

[confirmed] 7. Patent's IR bank uses ~128-256 amplitude-graduated IRs, runtime selection via 7-bit truncation for 128 levels.
  evidence: Quote verified independently via FreePatentsOnline.com (a source independent of and different from the cited Google Patents mirror): 'The appropriate response to use for any sample can be simply obtained by truncation of the magnitude of the sample to 7 bits (equivalent to 128 levels).' The same independent source also contains 'Typically a set of 128 or 256 impulse responses are used,' directly corroborating the '~128-256' figure.

[confirmed] 8. Selection between two bracketing amplitude-level IRs is a genuine proportional crossfade, not a hard switch — anti-click mechanism, per the 'quarter of the way... three-quarters/one-quarter' example.
  evidence: Quote verified verbatim via FreePatentsOnline.com (independent of the cited Google Patents source): 'a sample (14) a quarter of the way between two sample thresholds (15, 16) is applied three-quarters to the impulse response representing the lower sample level (17) and one quarter to the impulse response representing the higher sample level (18).'

[confirmed] 9. Patent claims generalize the blend as proportion dependent on position within a predetermined amplitude range — an explicit crossfade-by-position architecture.
  evidence: Substance verified via FreePatentsOnline.com (independent source): 'the proportions of the impulse responses applied to the input signal are dependent on the position of the amplitude of the input signal within the predetermined range.'
  CORRECTION: Trivial wording variance: the independent mirror renders this as '...are dependent...' (assertive, likely from the issued claim language) versus the cited quote's '...may be dependent...' (permissive, likely from the specification/description text) — both mirrors of the same patent describe the identical concept, so this is not a substantive discrepancy.

[confirmed] 10. Generalized envelope tracker (attack/decay leaky integrator, ~attack constant 10, decay constant 1000) can replace instantaneous magnitude as IR-selection driver, at cost of possible added latency to re-align timing.
  evidence: Quote verified verbatim via FreePatentsOnline.com (independent source): 'Typical values of attack and decay are 10 and 1000 respectively resulting in a rapid adoption of a higher level impulse response when the input signal increases in general amplitude coupled with a slower return to lower amplitude values when the general signal level decays.'

[confirmed] 11. Patent specifies remedial noise-floor-gated splice blending in higher-amplitude IR content below a threshold ~50% above noise floor, extending down to ~-12dB below max.
  evidence: Quote verified via FreePatentsOnline.com (independent source): 'It is appropriate to start the process at the impulse response some 12 dB below the maximum...' and 'The threshold can thus be set say 50% above this [noise floor].' Both figures match the claim.

[confirmed] 1. Direct-form (non-FFT) time-domain MAC convolution; 5,000-tap IR at 50 kHz costs ~250M MAC/s per IR-pair, parallelized across ~10 DSPs handling 500 taps each (US 7,039,194 B1, Kemp).
  evidence: Independently retrieved the patent text from a second, independent digitization (freepatentsonline.com, distinct from the cited Google Patents copy) and it reproduces the quote verbatim: 'Each output sample will require the accumulation of 5,000 values of input sample multiplied with 5,000 impulse response samples, or 250 million operations per second assuming a 50,000 sample per second sampling rate,' and 'ten processors could be used so that each performs 500 accumulation steps per output sample, and DSP 51 then has to sum the 10 partial values.' The same patent (via freepatentsonline) confirms the algorithm is pure time-domain sample-by-sample MAC ('multiply each element of the impulse response buffer... accumulation into each element of the output sample buffer') with no FFT/frequency-domain partitioning mentioned anywhere, and separately describes two impulse responses (a, b) being blended per sample via r=e.a+(1-e).b — supporting the 'per active IR-pair' framing. Patent metadata (priority GB9616755.6 9 Aug 1996, PCT/GB97/02159 filed 8 Aug 1997) also corroborated via a freepatentsonline/USPTO-derived search snippet independent of the cited Google Patents page.

[confirmed] 2. Bilinear interpolation solves analytically for blend factor j from linear gains Ma=10^(a/20), Mb=10^(b/20) so Mg=j.Ma+(1-j).Mb exactly equals desired gain, rather than interpolating dB directly (WO 2000/028521 A1 / US 7,095,860 B1, Kemp).
  evidence: Independently fetched US7095860B1 via freepatentsonline.com (a different source/OCR from the cited Google Patents page) which reproduces verbatim: 'Ma=10(a/20), and Mb=10(b/20)' and 'j=(Mg-Mb)/(Ma-Mb)', with Mg derived from the linear-amplitude combination rather than dB averaging -- consistent with the claim's technical characterization.

[confirmed] 3. Compression-ratio interpolation between curves N:1 and M:1 uses rational blend a=N(M-R)/(R(M-N)), not linear interpolation in R or dB (same patent family).
  evidence: Independent freepatentsonline.com fetch of US7095860B1 reproduces verbatim: 'first determine the coefficient of linear interpolation a from the formula a=N(M-R)/(R(M-N)).'

[confirmed] 4. Extrapolating toward infinite ratio (hard limiting) reduces the formula to a=N/R (same patent family).
  evidence: Independent freepatentsonline.com fetch of US7095860B1 reproduces verbatim: 'In the case of interpolating to an infinite ratio, the formula...needs to [be] adjusted as follows: a=N/R.'

[confirmed] 5. Impulse capture at a fixed gain-reduction point uses a ~100-sample (48 kHz) settle window after removing the tone, then records only ~200 samples total, because the window is short enough that gain reduction hasn't moved appreciably (same patent family).
  evidence: Independent freepatentsonline.com fetch reproduces verbatim: 'gain control elements demonstrate comparatively short impulse responses and so the entire impulse response may be captured before the gain has significantly changed. In practice it is found a settling time T2 of about 100 samples at 48 kHz is sufficient and a storage time T3 of 200 samples is sufficient...' This directly supports both the numeric figures and the stated rationale in the claim.

[confirmed] 6. Runtime envelope detector is an asymmetric one-pole with attack/release coefficients k1,k2 derived from time constants T1,T2 via (1-k1)^r=exp(-1/T1), k2^r=exp(-1/T2), standard exponential-decay coefficient matching (same patent family).
  evidence: Independent freepatentsonline.com fetch reproduces verbatim: 'E=E+k1.(|S|-E)' (attack), 'E=k2.E' (release), and '1-k1 raised to the value r gives exp(-1/T1) and k2 raised to the value r gives exp(-1/T2)' where r is sample rate -- matching the claim exactly.

[confirmed] 7. Two alternative architectures: (1) leave gain embedded in each FIR set so j alone reproduces the gain curve, or (2) normalize all FIRs to unity gain and apply gain reduction as an independent multiplicative stage post-convolution (same patent family).
  evidence: Independent freepatentsonline.com fetch reproduces both methods verbatim: Method 1 -- 'leave the impulse response (or sets of them) as sampled, with their inherent attenuation'; Method 2 -- 'eliminate the inherent attenuation from the sampled impulse responses' via normalization, then 'apply the appropriate gain reduction independently.'

[confirmed] 8. The Comunità/Steinmetz/Reiss paper explicitly classifies Volterra series and dynamic convolution together as the main non-differentiable black-box methods.
  evidence: I independently extracted the full text of the actual PDF myself (via PyMuPDF, bypassing any WebFetch summarizer) rather than relying on the cited page's rendering. Section 2.3 states verbatim: 'The main non-differentiable black-box methods are based on Volterra series and dynamic convolution.' Exact match.

[refuted] 9. The paper states Volterra methods are accurate only for weakly nonlinear systems and not applicable to most nonlinear audio effects, which is presented as motivation for pursuing black-box/gray-box neural AND dynamic-convolution alternatives.
  evidence: First half confirmed by direct PDF extraction: 'this method is sufficiently accurate only for weakly nonlinear systems, making it not applicable to most nonlinear audio effects' (independently, other literature such as the DAFx paper 'On the Use of Volterra Series for Efficient Real-Time Simulations of Weakly Nonlinear Analog Audio Devices' corroborates that Volterra is conventionally framed as a weak-nonlinearity tool). However, the second half misrepresents the source: the very next sentence in the paper is 'The same applies to dynamic convolution... which adjusts impulse responses or processing kernels...' -- i.e., dynamic convolution is presented as suffering the SAME accuracy limitation as Volterra series, not as an alternative solution motivated by Volterra's shortcomings.
  CORRECTION: Volterra's stated weak-nonlinearity limitation is not framed as motivating a move to dynamic convolution; the paper groups dynamic convolution with Volterra series as an equally limited non-differentiable black-box method ('the same applies to dynamic convolution'). The paper's actual motivation for later differentiable/gray-box neural methods is the shared inadequacy of both Volterra series and dynamic convolution, not Volterra's limits specifically motivating dynamic convolution.

[confirmed] 10. Dynamic convolution (per Kemp 1999) models nonlinear/hysteretic effects by deriving multiple IRs at multiple impulse amplitudes and selecting/adjusting the applied IR based on input amplitude over time.
  evidence: Direct PDF extraction confirms: 'dynamic convolution (Kemp, 1999; Primavera et al., 2012), which adjusts impulse responses or processing kernels based [on] the present/past input amplitude to model nonlinear or hysteretic behavior.' The 'multiple impulse amplitudes' detail is independently corroborated by Kemp's own AES paper title (confirmed via AES E-Library, see claim 11) and by the Primavera et al. dynamic-convolution literature describing IR measurement 'at various harmonic orders'/amplitude levels used in a multi-convolution/crossfade process.

[confirmed] 11. Kemp's 1999 paper is exactly: 'Analysis and Simulation of Non-Linear Audio Processes Using Finite Impulse Responses Derived at Multiple Impulse Amplitudes,' AES 106th Convention, Munich, May 1999, preprint 4919.
  evidence: Independently verified directly against the AES E-Library's own entry (aes.org/e-lib/browse.cfm?elib=8261, a primary/authoritative source distinct from the cited Frontiers article): Title 'Analysis and Simulation of Non-Linear Audio Processes Using Finite Impulse Responses Derived at Multiple Impulse Amplitudes', Author Michael J. Kemp, Paper Number 4919, Convention AES 106 (May 1999). The Frontiers paper's own reference list (independently extracted from the PDF) matches: 'Kemp, M. J. (1999)... Audio Engineering Society Convention 106, Munich, Germany, May 8-11, 1999.' All details (title, author, venue, dates, preprint number) match exactly across two independent sources.

[confirmed] 1. 2012 follow-up by Primavera, Cecchi, Gasparini, and Piazza applies PCA to the amplitude-dependent IR bank from dynamic convolution to reduce memory/compute, guitar preamp case study, EDERC 2012.
  evidence: Independently corroborated by the University lab's own publications page (a3lab.dii.univpm.it), IEEE Xplore (document 6532219, DOI 10.1109/EDERC.2012.6532219), and ResearchGate — all describing the paper as an efficient DSP implementation of dynamic convolution + PCA to lower workload/memory, guitar pre-amplifier case study, 5th EDERC 2012, Amsterdam, Sept 2012, pp. 30–34. Core substance holds.
  CORRECTION: Two imprecisions: (1) the author byline is actually five names — A. Primavera, S. Cecchi, L. Romoli, M. Gasparini, F. Piazza (per ResearchGate/IEEE) — the claim's 'Primavera, Cecchi, Gasparini, and Piazza' omits co-author L. Romoli. (2) The canonical IEEE/EDERC title is 'An Efficient DSP-Based Implementation of a Dynamic Convolution Approach Exploiting Principal Component Analysis'; the shorter variant quoted in the claim ('...implementation... using principal component analysis') matches only a self-archived academia.edu copy, not the DOI-indexed published title.

[confirmed] 2. The paper's taxonomy places black-box modeling on a spectrum from non-differentiable classical DSP (Volterra series, dynamic convolution) to differentiable NN black-box models, distinct from gray-box models embedding partial physical structure.
  evidence: Direct fetch of the published Frontiers in Signal Processing article (10.3389/frsip.2025.1580395) shows a Section 2.3 on non-differentiable black-box methods explicitly covering Volterra series ('output is given by a sum of convolutions of the input signal with a series of kernels') and dynamic convolution ('adjusts impulse responses...based on present/past input amplitude'), with the paper's stated black-box/gray-box distinction matching the quoted definition.

[confirmed] 3. PCA post-processing on Kemp-style dynamic convolution: M=64 amplitude levels (1 dB apart, 48 kHz), arranged as an L×M matrix, PCA/eigen-decomposition giving Ĥ = V·W with far fewer than 64 components.
  evidence: Independently confirmed via two separate self-archived artifacts by the same authors (SlideShare poster 'Approximation of Dynamic Convolution...' and a separate SlideShare conference presentation 'Dynamic Convolution'), both giving Ĥ = V·W, M = 64 levels, 1 dB steps, 48 kHz sample rate — consistent across both independent documents.

[confirmed] 4. Per-sample level-selection function S(x[n]) = 1 + {|x[n]|/(fs/M)}.
  evidence: Verbatim formula recovered independently from both the SlideShare poster and the separate SlideShare presentation document.

[confirmed] 5. Realized as K parallel branches of (waveshaper) × (FIR filter), waveshaper curves = PCA eigenvectors, FIR coefficients = principal-component weights, replacing full 64-IR switching/interpolation.
  evidence: Consistent with independently fetched formula Ĥ=V·W where V = eigenvectors of the covariance matrix (used as basis/waveshaper coefficients) and W = V^T·H (principal components, used as filter coefficients), as described in both SlideShare artifacts; this rank-reduced factorization architecture is the standard way such PCA decompositions are realized in real time.

[confirmed] 6. Validated on 3 hardware devices (Aguilar bass pre-amp, BOSS DS-2, dbx compressor) at light/medium/heavy nonlinearity, with MSE/variance-explained curves plus MUSHRA (10 subjects, 7M/3F).
  evidence: Independently confirmed via direct fetch of the SlideShare poster, which explicitly lists all three devices, the three nonlinearity strengths, and quotes '10 subjects (7 male and 3 females)' for the MUSHRA test — matching the claim's quote precisely.

[confirmed] 7. Companion report: 1–3 retained PCs sufficient for very low MSE (~2.59×10⁻⁷) on the guitar/bass pre-amp case.
  evidence: Independently confirmed via a separate SlideShare presentation document (distinct from the poster) giving an explicit table: 1 PC → 1.67×10⁻⁵, 2 PCs → 1.27×10⁻⁶, 3 PCs → 2.59×10⁻⁷ MSE, exactly matching the claim's number.

[confirmed] 8. AES elibrary page confirms Kemp's paper describes bilinear dynamic convolution varying sample-by-sample, based on level-dependent IR measurements plus a measured attenuation-vs-amplitude characteristic.
  evidence: Direct fetch of the AES e-library abstract page (id=9153) for Kemp, 'Analysis and Simulation of Analogue Dynamic Compressors and Limiters in the Digital Domain,' AES 109th Convention, Preprint 5185 (Sept 2000), returns exactly this description: level-dependent IR measurements at multiple attenuation levels, an attenuation-vs-amplitude characteristic, and 'a bilinear dynamic convolution...varying on a sample-by-sample basis.' Corroborated further by independent Sintefex/Kemp biographical material describing the same patented technique.

[confirmed] 9. Patent US7095860B1 discloses selecting the active IR per sample by truncating input magnitude to a small number of bits, up to 128 discrete levels.
  evidence: Confirmed not only in US7095860B1 itself but independently corroborated in the related but distinct Kemp/Sintefex patent US7039194B1 ('Audio effects synthesizer with or without analyzer'), which contains the identical text: 'truncation of the magnitude of the sample to 7 bits (equivalent to 128 levels).' Two separate patent documents from the same inventor/family carrying identical technical content constitutes solid corroboration.

[confirmed] 10. Dynamic convolution is genuinely bilinear/2D: factor k interpolates between adjacent amplitude-indexed IRs, factor j interpolates between two separately measured FIR sets (e.g., different ratio/gain-reduction settings), combining four IR taps per output sample.
  evidence: US7095860B1 text: 'a single value...is derived from the 4 elements of the 4 impulse responses by bilinear interpolation using values j and k...determined on a sample by sample basis.' Independently corroborated by US7039194B1, which describes the same mechanism as 'a two-dimensional, or bilinear, interpolation between 4 impulse responses with one dimension dependent on signal amplitude and the other dependent on the other parameter,' and notes it can be extended to more simultaneously-varying parameters.

[confirmed] 11. Because IR selection/interpolation happens fresh every sample (no block crossfade), cost is large: ~5000-tap IRs at 50 kHz require ~250 million MACs/sec per channel, needing multiple DSPs per the patent.
  evidence: US7095860B1: 'accumulation of 5,000 values of input sample multiplied with 5,000 impulse response samples, or 250 million operations per second assuming a 50,000 sample per second sampling rate.' Independently corroborated verbatim in the separate patent US7039194B1, which gives the identical figure and IR-length context (typical 5,000-tap IRs, useful results down to 50–200 taps for effects needing less resolution, e.g. tape/valve). The 'needing multiple DSPs' framing is a reasonable inference from the stated MAC/sec figure relative to typical DSP throughput of that era but is not itself a verbatim patent claim.
  CORRECTION: The 'needing multiple DSPs for real-time operation as described in the patent' clause is not literally stated in the patent text located; it is a plausible inference from the 250M MAC/s figure rather than a directly quoted patent assertion.

[plausible] 1. Kemp AES 5185 / US7095860 patent: measured IR lengths for compressor/limiter emulation range 50 to ~5000 samples depending on device, implying up to ~100ms per gain-reduction setting. Quote: 'Typical: 5,000 samples per impulse response; Range: 50-5,000 samples depending on device characteristics'
  evidence: Independently fetched the actual US7095860B1 patent text (Google Patents + downloaded PDF/OCR). The patent DOES support the substance: 'useful results can be obtained with responses as short as for example 50 to 200 steps'; 'valve processors and tape-recorders... responses as short as 200 samples'; 'the length of the impulse responses used (M) may typically be 5,000 or longer'; 'impulse times of up to 1/10th second, or for example, 5,000 samples'; and elsewhere 'n... Although 4000 is a typical value'. So a 50-to-~5000-sample range across device types is a fair synthesis, and 5000/44100Hz≈113ms rounds to 'roughly 100ms'. HOWEVER, the specific quoted text given ('Typical: 5,000 samples per impulse response; Range: 50-5,000 samples depending on device characteristics') does NOT appear verbatim anywhere in the patent — it is a paraphrase dressed up as a direct quote. The patent instead states these figures in several separate, non-contiguous sentences (50-200 for one device class, 200 for another, 4000/5000 as typical M/n elsewhere). Treat as substantively correct but the 'quote' is fabricated/synthesized, not literal.
  CORRECTION: The substantive range (roughly 50-5000 samples, i.e. up to ~100ms) is corroborated, but no sentence in US7095860 literally reads 'Typical: 5,000 samples...Range: 50-5,000 samples depending on device characteristics.' The patent's actual wording is fragmented: '50 to 200 steps' (minimum useful), '200 samples' (valve/tape), '5,000 or longer' (typical M), '4000... typical value' (n), '5,000 samples' (1/10s reverb-like example).

[confirmed] 2. A gain-characteristic threshold can be shifted without re-measurement by subtracting the dB difference (A-B) between original and user-selected threshold from the derived envelope before it indexes into the gain/IR table. Quote: 'the amount (A−B) dB is subtracted from the derived input signal envelope'
  evidence: Independently fetched US7095860B1 (Google Patents) text, which contains the sentence verbatim: 'If the original measurements of the gain characteristic were taken at a threshold of −A dB and the user now desires a threshold of −B dB the amount (A−B) dB is subtracted from the derived input signal envelope.' Matches the claim's mechanism and quote exactly.

[refuted] 3. 'test claim' cited to William G. Gardner, 'Efficient Convolution without Input-Output Delay,' JAES vol. 43, no. 3, pp. 127-136, March 1995
  evidence: The citation itself (Gardner, JAES 43(3):127-136, March 1995, the well-known 'double buffering'/zero-latency partitioned convolution paper) is a real, correctly identified publication. However, the 'claim' text supplied is literally the placeholder string 'test claim' with an attached quote fragment that is not a real quotation of any content — it does not correspond to any statement, finding, or figure in Gardner's paper. There is nothing substantive to verify; as stated, the claim is empty/non-existent content misattributed to a real paper.
  CORRECTION: No actual technical claim was made — 'test claim' is not a genuine assertion drawn from Gardner (1995) and should be discarded/replaced with an actual excerpted claim if one is intended.

[confirmed] 4. Click-free IR exchange via output crossfading (not coefficient crossfading): same input block convolved with old and new IR, outputs crossfaded with complementary envelopes. [Wefers PhD thesis]
  evidence: Independently downloaded and OCR'd the actual Wefers 2014/2015 thesis PDF (via the Wayback Machine copy, fetched directly with curl since WebFetch tool also failed on archive.org for us, corroborating the cited access difficulty). Section 4.4 'Output crossfading' contains the exact equations and wording quoted: 'this signal block is filtered with both impulse responses, resulting in two intermediate output signals y0(n)=x(n)∗h0(n) and y1(n)=x(n)∗h1(n)... both intermediate output signals are crossfaded using envelopes fout(n) and fin(n)... y(n)=y0(n)·fout(n)+y1(n)·fin(n) if n<L.'

[confirmed] 5. Typical crossfade lengths L are 8-32 samples for binaural/HRTF filtering, up to a full block (L=B) for room-IR time-varying filtering. [Wefers PhD thesis]
  evidence: Verified directly in the extracted thesis text, same page as claim 4: 'The length L of the crossfade is application specific. Typical values in binaural synthesis are between 8 and 32 samples. For time-varying filtering with room-impulse responses, L can be longer and even L = B.' Verbatim match.

[confirmed] 6. Naive time-domain output crossfading increases per-sample compute cost by 50-60% over static (time-invariant) filtering. [Wefers PhD thesis]
  evidence: Verified directly in extracted thesis text (section 4.4.1 Performance, B=128, L=32 example table 4.3 discussion): 'time-varying filtering demands 50-60% more computation than a time-invariant filtering.' Verbatim match; thesis also notes the theoretical ideal-case lower bound would be (B+L)/B = 25% if only the crossfade itself were costed, underscoring that 50-60% is the actual (non-ideal) overhead.

[confirmed] 7. Wefers's own frequency-domain crossfading method embeds output crossfade in the OLS discard zone via periodic cosine-square envelopes with 3 nonzero real DFT coefficients, 12 real ops/DFT bin, eliminating the 2nd inverse FFT, reducing time-varying overhead to 17-34% vs 50-60%. [Wefers PhD thesis]
  evidence: Verified directly: text states the method is from 'The author proposed... [123]' (a prior AIA-DAGA 2013 contribution, generalized in the thesis) — matches claim's parenthetical. Confirmed exact figures: 'Eq. 4.50 requires twelve arithmetic operations per DFT coefficient: three complex-valued additions, two complex-valued subtractions and one complex-by-real multiplication'; 'They contain only three non-zero coefficients, which are all real-valued' (re: cosine-square envelope spectra); 'the increase in costs for a time-varying filtering range only between 17-34%. This is a strong improvement over the time-domain crossfading algorithms presented before, which were 50-60% more expensive.' All verbatim matches.

[confirmed] 8. Non-uniform partitioned convolution filter exchange trade-off: coherent exchange has worst-case response latency up to the coarsest cycle; asynchronous exchange gives constant ~1-cycle response time but uneven/discontinuous transition unless crossfade scaled for longer segments. [Wefers PhD thesis Ch.6.11]
  evidence: Verified directly in extracted thesis text (section 6.11, pp.188-189): 'the filter exchange is subject to a trade-off between a coherent transition and the response time... For the strategy of a coherent exchange, the transition time is constant and can be adjusted by the crossfade length, whereas the response time can fluctuate...'; 'an asynchronous filter exchange results in a constant response time, but an unpredictable transition time, which can in the worst-case be as long as the longest segment.' Verbatim match. Note: the thesis itself attributes originating this two-strategy framework to Müller-Tomfelde [ref 72] ('presumably the first who addressed the exchange of filters in non-uniformly partitioned convolution algorithms... The findings of his paper are briefly reproduced in the following'), with Wefers reproducing/illustrating it (Fig. 6.18) — the claim's citation to the Wefers thesis pages is accurate for locating this passage, but the underlying idea's priority belongs to Müller-Tomfelde, not Wefers.
  CORRECTION: Add attribution nuance: this coherent-vs-asynchronous trade-off framework was originated by C. Müller-Tomfelde (cited by Wefers as ref [72]) and is reproduced/illustrated (not originated) in the Wefers thesis at the cited location.

[confirmed] 9. Cycle-count case study, 2.0s RT60 IR (88200 taps), 44.1kHz, B=128: UPOLS 2018 cycles/sample, up to 345Hz IR update rate; NUPOLS 204.1 cycles/sample (9.9x, zero slack) or 240.4 cycles/sample schedulable (8.4x, +18%, ~700µs filter transform). [Wefers PhD thesis Ch.6.12]
  evidence: Verified directly in extracted thesis text (section 6.12, p.191), all figures match verbatim: 'A reverberation time of 2.0s, a sampling rate of 44100Hz and a streaming block length of B=128 samples. The destination filter length is N=88200 taps... The standard UPOLS algorithm partitions the filter into 690 length-128 sub filters. Each filtered output sample consumes 2018 CPU cycles. A filter transformation of the entire impulse response takes approximately 700µs... The filter update rate of 345Hz widely exceeds the throughput of the GA simulations... The NUPOLS algorithm, operated with this partition, has a total cost of 204.1 CPU cycles per filtered sample. This is 9.9x more efficient... However, all segments in this partition have zero clearance... Operating the NUPOLS algorithm with P requires 240.4 CPU cycles per sample. This increases the costs by 18%, but is still 8.4x more efficient than a uniform partition.'

[confirmed] 10. Optimal-partition search in Garcia's AES paper 5660 is formalized as a Viterbi/dynamic-programming shortest-path over a state graph, not closed-form convex derivation. Quote: 'The state sequence that represents the optimal partition is found using the Viterbi algorithm.'
  evidence: Independently downloaded the actual Garcia AES Convention Paper 5660 PDF and extracted its text via pdfminer (bypassing the same PDF-parsing obstacle noted in the citation). Found verbatim: 'a partition is represented as a sequence of states... The state sequence that represents the optimal partition is found using the Viterbi algorithm... the rest (i.e. the Viterbi algorithm) is a mechanical procedure that consists of evaluating partially-optimal state sequences of gradually increasing length, and finally backtracking through the optimal sequence.' Exact match; also confirmed correct paper metadata: 113th AES Convention, LA, 2002 Oct 5-8, Paper 5660, author Guillermo García (Creative ATC / CCRMA Stanford).

[confirmed] 11. States defined as [S.Q] pairs: S = block-size multiplier (units of header block N), Q = which N-sample sub-fraction of that block the pointer sits at. Quote: 'the state is defined by the following two parameters: 1. The size of the block the pointer falls within... 2. The fraction of the block the pointer points to'
  evidence: Verified directly from the extracted Garcia 5660 text: 'the state is defined by the following two parameters: 1. The size of the block the pointer falls within (e.g. a block of 4N points). 2. The fraction of the block the pointer points to (e.g. the third quarter of a 4N-point block). We will denote the state as [S.Q] with S and Q positive integers, indicating that the pointer falls at the beginning of the Qth N-point fraction of a block of size SN. For example, a state noted [4.3] indicates that the pointer falls at the beginning of the 3rd quarter of a block of size 4N.' Exact verbatim match including the [4.3] example.

[confirmed] 1. Legal partition rules: power-of-two block sizes; block of size S cannot start before sample S into the IR; a block of size S can only be followed by a block of equal-or-greater size. (García, AES Convention Paper 5660, 113th AES Convention, 2002)
  evidence: Independently downloaded the PDF directly from angelofarina.it via curl (bypassing WebFetch, which also failed to parse it for me) and extracted text locally with pdfminer.six. Lines 1067-1072 of the extracted text read verbatim: '1. Block sizes must be integer powers of two. 2. A block of size S cannot start before the Sth sample into the filter impulse response [4]. 3. A block of size S can only be followed by a block of size equal or greater than S.' Independently corroborated by Frank Wefers' PhD dissertation ('Partitioned convolution algorithms for real-time auralization', RWTH Aachen), which describes García 2002 as requiring sub-filter lengths to form 'a non-decreasing sequence Ni ≤ Ni+1' — the same constraint in different notation.

[confirmed] 2. Transition costs: 0 for intra-block advance, 4 madds for extending current FDL, 4k·log2(2YN)+4 for a full new FDL, reduced to (1+2/P)k·log2(2YN)+4 via half-spectrum reuse when new block is exactly double size and P channels share the frequency-domain mix. (García 2002)
  evidence: Verified verbatim in the independently re-extracted PDF text: 'cost(X.X->X.1) = 4' (intra-FDL extension), 'cost(X.Q->X.(Q+1); Q<X) = 0' (intra-block advance), 'cost(X.X->Y.1; Y>X) = 4k log2(2YN) + 4' (new FDL), then for P parallel channels 'cost(X.X->Y.1; Y>X) = (2+2/P) klog2(2YN) + 4', and for the Y=2X half-spectrum-reuse special case 'cost(X.X->Y.1; Y>X) = (1+2/P) klog2(2YN) + 4' — an exact match. I also independently re-derived this arithmetically from the paper's own OFFT=2k·log2(2N) per-FFT cost definition: forward-FFT halved (k log2) + inverse-FFT shared across P channels (2k log2/P) + 4 madds spectral multiply = (1+2/P)k log2(2YN)+4, internally consistent.

[confirmed] 3. For the 131072-sample (~3s) IR at 256-sample (5.8ms) latency, k=1.5, optimal partition is 3 FDLs (8×256 + 7×2048 + 7×16384) costing 304 madds/sample, vs. 769/964 madds for Gardner's MBMC/MBUL — over 2x improvement. (García 2002)
  evidence: Verified verbatim in the independently extracted PDF text: 'the optimal partition found by our algorithm consists of three FDLs: 8 blocks of 256 samples, 7 blocks of 2048 samples and 7 blocks of 16384 samples... cost per output sample... is of 304 multiply-adds... The algorithms given in [4] would cost 769 madds for the minimum-cost version (MBMC), and 964 madds for the uniform-load version (MBUL)', with the paper's own text stating the multiple-FDL algorithm is 'more than two times more efficient' — 769/304≈2.53x and 964/304≈3.17x, both consistent with 'over 2x'. Reference [4] in García's paper is confirmed to be Gardner, 'Efficient convolution without input-output delay,' JAES 43(3), 1995.

[confirmed] 4. Same test case: 70,055,503 possible legal partitions, costs spanning 308-2402 madds, making exhaustive search infeasible for real-time/periodic rescheduling as IR length changes -- motivating the DP/Viterbi algorithm. (García 2002)
  evidence: Verified verbatim in the independently extracted PDF: 'there are 70,055,503 possible partitions. Using a typical FFT implementation (k=1.5), the cost per output sample ranges from 308 madds for the optimal partition to 2402 madds for the least-efficient partition.' The motivation clause is also verified: the same passage continues 'performing an exhaustive search over all... possible partitions [would] be computationally very expensive, especially when the partition needs to be updated periodically in order to track time-varying filter lengths' — directly supporting the claim's framing.

[confirmed] 5. Stepwise partition-at-a-time IR replacement produces output equivalent (67.2 dB SNR difference) to crossfading two full time-invariant convolutions, at zero added cost, because convolution's inherent triangular overlap ramp does the crossfading implicitly. (Brandtsegg & Saue, DAFx-17)
  evidence: Independently downloaded the DAFx-17 paper PDF directly and extracted text with pdfminer. Verbatim match: 'An estimate of the signal-to-noise ratio (SNR) returns 67.2 dB... convolution with stepwise IR replacement preserves the behavior of time-invariant convolution, while at the same time allowing filter updates within a single process at no extra cost.' The 'inherent ramp/implicit crossfade' mechanism is also explicitly stated earlier in the paper (Fig. 1 discussion): 'the ramp characteristics caused by the gradual overlap... This inherent "fade in/fade out"-property of convolution will be exploited in our approach.' Minor nuance: the paper's baseline reference is built by literally splicing/merging two separately-run time-invariant convolutions at the switch point (not an explicit crossfade operation) — the overlap that produces the crossfade-like blend arises from the convolution tails, which is consistent with, but not verbatim identical to, calling it 'crossfading two full time-invariant convolutions.'

[confirmed] 6. IR replacement must be quantized to partition boundaries (nT=k·NP) and takes exactly P-1 partitions, during which output is a genuine sum of old/new contributions, with total multiply-add count unchanged from static convolution. (Brandtsegg & Saue, DAFx-17)
  evidence: Verbatim in independently extracted text: 'the replacement time nT must be at a partition border: nT = k ∗ NP for k = 0, 1, 2, ...' and 'In a transition period equal to P − 1 (where P is the number of IR partitions) the output is a mix of two convolution processes' and 'there is no extra cost, since it is solved by gradual coefficient replacement, and the total number of additions in equation 12 is equal to N − 1 at every step n.' The 'no extra cost' claim is reaffirmed later specifically for the partitioned/FFT implementation: 'the proposed method does not add any extra computational cost. Impulse response partitions are simply replaced in the right order.'

[confirmed] 7. Property 2 (partition pair i,j contributes to Y_k only if i,j≤k; to Y_{P+k} only if i,j>k, '>' being an OLA-specific correction) jointly justifies independent partition-by-partition load/unload without discontinuity. (Brandtsegg & Saue, DAFx-17)
  evidence: Verbatim confirmed in independently extracted text. Property 1: 'contributes to output interval Yk only if i, j ≤ k.' Property 2 (initial statement): 'contributes to output interval YP+k only if i, j ≥ k' — then explicitly refined later: 'since this contribution is buffered with the output of interval Yi+j, it is not necessary to recalculate the contributing convolution during interval Yj+i+1. For that reason the final inequality of Property 2 can be adjusted from ≥ to >: The partitions xi(n) and hj(n) contributes to output interval YP+k only if i, j > k.' This confirms the claim's characterization that the '>' is a downstream OLA-buffering correction to the originally-stated '≥', not the paper's initial Property 2 statement verbatim -- the claim accurately reflects this two-step derivation.

[confirmed] 8. The same load-partition-by-partition mechanism lets convolution start before the full IR is captured, cutting IR-load latency to one partition length and spreading FFT computation of the new IR over time instead of a CPU burst. (Brandtsegg & Saue, DAFx-17)
  evidence: Verbatim confirmed: 'A fully loaded IR is not necessary to initiate the convolution process since the later partitions initially do not contribute to the convolution output. This drastically reduces the latency e.g. for application of live sampled impulse responses. Convolution can start during sampling, as soon as a single partition of the IR is available. In addition the computational load associated with FFT calculations on the IR is conveniently spread out in time, hence avoiding bursts of CPU usage when loading long impulse responses.'

[confirmed] 9. Validation test used NP=1024, NB=2048 (2·NP OLA), 1-second 44,100 Hz speech IRs (N=44100, P≈43 partitions), a 10-second baroque-music source, transitioning at nT≈4.5s. (Brandtsegg & Saue, DAFx-17)
  evidence: Verbatim confirmed for all directly-stated figures: 'all convolution operations are running with partition length NP = 1024 and FFT block size NB = 2048,' 'both 1 second excerpts of speech sampled at 44,100 Hz (N = 44100),' 'input source signal x(n)... is a 10 second excerpt of baroque chamber music, also sampled at 44,100 Hz,' and 'At time nT ≈ 4.5 seconds, a switch between impulse responses hA(n) and hB(n) is initiated.' The 'P≈43 partitions' figure is not stated explicitly in the paper but is a correct arithmetic derivation (44100/1024≈43.07) consistent with the paper's own P=N/NP definition, so it is a valid, independently-checkable inference rather than a paper quote.

[confirmed] 10. Disadvantage of the zero-cost stepwise scheme (vs. an explicit tunable crossfade like Wefers & Vorländer 2014) is that transition duration is fixed to full IR length N, not independently controllable. (Brandtsegg & Saue, DAFx-17)
  evidence: Verbatim confirmed: 'In contrast to our earlier attempts and also to the crossfade methods suggested by [16], the proposed method does not add any extra computational cost... A possible disadvantage is that the length of the transition interval is fixed to the filter length N and not available for user control.' Reference [16] in the DAFx-17 bibliography is confirmed to be 'Frank Wefers and Michael Vorländer,' matching the claim's contrast target.

[confirmed] 11. Wefers & Vorländer (Forum Acousticum 2014, earlier version AIA-DAGA 2013 Merano p.263) proposed blending filter crossfades on DFT-domain block outputs via circular convolution with a frequency-domain window, needing only one inverse FFT per output block instead of two.
  evidence: Independently corroborated via Frank Wefers' own PhD dissertation (downloaded and text-extracted separately), which cites reference [123] exactly as: 'F. Wefers and M. Vorländer. Frequency domain filter exchange for DFT-based fast convolution. In AIA-DAGA 2013: Conference on Acoustics including the 40th Italian (AIA) Annual Conference on Acoustics and the 39th German Annual Conference on Acoustics (DAGA), pages 263–263, Merano, Italy, 2013' -- confirming the AIA-DAGA 2013/Merano/p.263 citation exactly. The dissertation's own text on the technique states: 'The author proposed a method that integrates the time-domain crossfading... directly into the DFT domain [123]. The benefit is that the second inverse transform in the algorithm... is saved,' and describes it as circular convolution of DFT spectra with the DFT of fade envelopes ('Crossfading... in the time-domain corresponds to the K-point circular convolution of their DFT spectra... with the DFT transforms... of the fade envelopes') -- matching the claim precisely. A separate, independent web search corroborates the Forum Acusticum 2014 venue/date (Kraków, Poland, September 7-12, 2014, organized by the European Acoustics Association on behalf of the Polish Acoustical Society) though note the claim's spelling 'Forum Acousticum' differs from the conference's official name 'Forum Acusticum' (single 'o'), a minor spelling variance seen in some citations. The cited US Patent 10,187,741 B2 was independently confirmed to exist (USPTO PDF titled 'Device and method for processing a signal in the frequency domain,' inventor Andreas Franck of Fraunhofer IDMT) as stated.

[refuted] 1. The naive alternative to Wefers/Vorländer's method (two parallel fast convolutions + time-domain crossfade) costs an extra spectral convolution, an extra inverse transform, and crossfade arithmetic — quote attributed to US Patent 10,187,741 B2.
  evidence: I obtained an independent, non-image digitization of the same patent (Justia Patents, patents.justia.com/patent/10187741, full text ~262KB) and full-text-searched it for the word 'extra' — it appears ZERO times anywhere in US 10,187,741 B2. The patent's actual, differently-worded statement of the same idea (confirmed in both the Justia text and an independent Google-Patents extraction) is: 'a typical solution for filter crossfading includes two FD convolution processes using different filters and subsequently crossfading the outputs in the time domain' — no mention of 'extra spectral convolution and inverse transform... operations consumed by the crossfade.' That exact sentence, verbatim, is instead found in a genuinely independent source: Brandtsegg & Øyvind Saue, 'Live Convolution with Time-variant Impulse Response,' Proc. DAFx-17, Edinburgh, pp. 239-246 (I downloaded and text-extracted this PDF myself), which reads: 'The disadvantage of this method (assuming fast convolution in the frequency domain) is the added cost of an extra spectral convolution and inverse transform, as well as the operations consumed by the crossfade.' Brandtsegg & Saue cite this as their own characterization of the conventional baseline (in a paragraph discussing Wefers & Vorländer [ref 16] = Forum Acusticum 2014, confirmed present in their reference list), not as a quotation from the Wefers/Vorländer work or from the Franck patent.
  CORRECTION: The quoted sentence is misattributed. It is not present in US Patent 10,187,741 B2 (verified by full-text search of an independent digitization); it is verbatim text from Brandtsegg & Saue, 'Live Convolution with Time-variant Impulse Response,' DAFx-17 (2017), p. 240. The substantive point (double-convolution-plus-crossfade costs an extra spectral convolution/IFFT/crossfade arithmetic) is nonetheless accurate and is independently corroborated in substance by both the patent's own (differently worded) description and by Brandtsegg & Saue.

[confirmed] 2. Per the Franck patent, a full circular convolution of the crossfade window in the DFT domain is O(L^2), which only pays off vs. an extra IFFT (O(L log2 L)) if the frequency-domain window is sparse.
  evidence: Verified verbatim in an independent digitization of the patent (Justia full text, distinct from the USPTO image-PDF cited): 'a circular convolution of two sequences of a length L necessitates approximately L^2 complex multiplications and additions, which exceeds by far the potential gain of approximately O(L log2 L) due to the savings of an inverse FFT. If, however, the frequency-domain window W[k] contains only a few non-zero coefficients, the FD crossfading may become more efficient than the conventional time-domain implementation.' This matches the claim's quote essentially word-for-word and was independently cross-checked against a separate Google-Patents-based extraction of the same passage, which agreed in substance.

[plausible] 3. A sparse frequency-domain crossfade window with K=8 optimally-chosen coefficients (R={0,1,3,5,7}, I={2,4,6}) nearly matches a full dense window's accuracy, with L2/L∞ errors of 9.388e-6 / 7.102e-6.
  evidence: Two independent full-text extractions of the patent (Justia digitization; Google Patents extraction) both confirm the general design study exists: a 'complete set of 8 complex coefficients' (denoted K=15 in the patent's own K=|R|+|I| convention) achieves L2/L∞ errors of 9.37e-6 / 5.65e-6; a K=8 all-real design is far worse (5.45e-2/1.55e-2); and a separate K=8 design using specifically-chosen mixed real/imaginary coefficients (FIG. 6c) is stated to 'nearly reach the performance of the example with 8 complex coefficients' — qualitatively matching the claim. However, the exact numeric values (9.388e-6, 7.102e-6) and the specific index sets R={0,1,3,5,7}/I={2,4,6} could not be located in either independent text extraction — these figures most likely reside only in a summary table (FIG. 7g, described in-text as 'the designs are summed up in FIG. 7g') that is an embedded image not captured by OCR/text-layer extraction, so I could not independently verify the exact digits. The claimed numbers are internally consistent with what was found (slightly worse than the K=15 case, as expected for K=8) but remain unconfirmed by direct citation.

[confirmed] 4. For N=512, B=128, 8 sources, moving crossfade to DFT domain + summing spectra before a shared IFFT cuts per-sample instructions from ~186 to ~131 for one channel.
  evidence: Verified verbatim in an independent digitization of the patent (Justia full text): 'A rendering system with a filter length N=512, a block size B=128 and the corresponding standard DFT size L=256, M=8 virtual sources and K=4 non-zero coefficients... summation of the ear signals in the frequency domain results in considerable additional reductions in complexity, that is from ≈186 to ≈131 instructions per sample for N=512.' This matches the claim's setup (N=512, B=128, 8 sources) and figures (186→131) exactly.

[refuted] 5. The DFT-domain-crossfade technique is explicitly positioned in the literature as one of only two known prior alternatives to the costly double-convolution-crossfade baseline, the other being Brandtsegg & Saue's own zero-cost partition-replacement scheme.
  evidence: I directly examined the independent primary source for this framing, Brandtsegg & Saue, DAFx-17 (2017) (downloaded and text-extracted myself). It says: 'Wefers and Vorländer [16] points at two possible solutions to time-varying FIR filtering: instantaneous switching or crossfading' — i.e., the 'two possible solutions' referenced are instantaneous switching vs. crossfading, both described within Wefers & Vorländer's own paper, not 'two known prior alternatives to the costly baseline' with Brandtsegg & Saue's scheme as the second. Later, B&S write 'In contrast to our earlier attempts and also to the crossfade methods suggested by [16], the proposed method does not add any extra computational cost' — here B&S position their own new method as superior to (a) their own prior work and (b) Wefers/Vorländer's crossfade family, treating W&V's technique as one item of background prior art their new method improves on, not as a co-equal 'alternative' paired with their own scheme against a shared baseline. No passage in the accessible literature asserts these are 'the only two known' alternatives.
  CORRECTION: Brandtsegg & Saue's DAFx-17 paper frames Wefers & Vorländer's DFT-domain crossfading as one of several prior-art approaches (instantaneous switching, plain time-domain crossfade, DFT-domain crossfade, and Brandtsegg/Saue's own earlier attempts) that their new stepwise partition-replacement method supersedes — not as one of exactly two known alternatives paired with their own scheme.

[confirmed] 6. TVOLAP costs the same as WOLA but its switching latency is exactly 1/M of WOLA's; e.g., at NIR=2048, 2L=512/M=4, TVOLAP gives 256-sample switching / 512-sample audio latency vs. WOLA's 1024/2048, for near-identical MFLOPS (14.592 vs 14.064).
  evidence: I independently downloaded and text-extracted the arXiv PDF myself (arxiv.org/pdf/2310.00319, via PyMuPDF, not relying on the citing agent's transcription) and located Table 1 verbatim: 'WOLA: audio lat. 2048, switching lat. 1024, MFLOPS 14.064' and 'TVOLAP: audio lat. 512, switching lat. 256, MFLOPS 14.592' — matching the claim exactly. I also algebraically re-derived the general 1/M relationship from the paper's own stated formulas (WOLA switching latency = NIR/2; TVOLAP switching latency = NIR/(2M)), confirming it holds generally, not just in the cited example. Independent corroboration of the algorithm's real-world use (not the specific numbers) comes from Jaeger, Bitzer, Simmer, Blau, 'Comparison of binaural auralisations...,' Acta Acustica (2025), an unrelated later paper that states headphone audio was computed using 'a time-variant partitioned convolution algorithm based on Jäger et al.'

[confirmed] 7. TVOLAP requires no crossfade code path for IR switching — all M transfer-function partitions are simply replaced in the ordinary spectral multiply-accumulate step, and the existing Hann-window overlap-add machinery alone produces click-free transitions.
  evidence: Confirmed verbatim in my own direct extraction of the arXiv PDF: 'Time-variant impulse responses can easily be incorporated. This is accomplished by simply exchanging the transfer function partitions H(k,m) by new ones... in the spectral multiplication step. It should be noted that all transfer function partitions must be exchanged at the same time.' This matches the claim's description precisely.

[confirmed] 8. OLA and OLS both click on IR switch (OLA via old/new-IR remainder mixing distortion, OLS via a hard cut); WOLA/TVOLAP crossfade smoothly via inherent Hann-windowed overlap-add; the gap between WOLA/TVOLAP and full CF-TDC is only 'small' and near-indistinguishable except in a difference signal.
  evidence: Confirmed verbatim in my own direct extraction of the arXiv PDF: 'OLA shows a high amount of distortions after a small, channel-dependent delay following the switching event... OLS does not use remaining outputs... immediately delivers the new output, resulting in a hard switch... producing audible artifacts, perceived as clicking sound. In contrast, WOLA shows a soft crossover... TVOLAP shows the same behavior as WOLA' and 'Differences between the two approaches [TVOLAP vs CF-TDC] are present but small — they become only visible when the difference signals are analyzed.' Matches the claim exactly.

[confirmed] 9. Exact FLOPS at NIR=2048, fs=48kHz: direct=196.608 MFLOPS, OLA=6.96 MFLOPS (145 ops/sample), WOLA=14.064 MFLOPS (296 ops/sample), TVOLAP=14.592 MFLOPS — TVOLAP ~ WOLA cost, ~13-14x cheaper than CF-TDC at equal quality.
  evidence: All headline figures (196.608, 6.96/145, 14.064, 14.592, '13 to 14 times more efficient than CF-TDC') were verified verbatim in my own direct PDF extraction and Table 1. I independently recomputed every formula: OLA 145*48000=6.96M ✓; TVOLAP (20·log2(512)+56+64+4)*48000 = 304*48000 = 14.592M ✓ exactly. One caveat found on recomputation: the paper's own printed WOLA formula '2·(145+3)·48000' does not arithmetically equal 14.064M if read with the parentheses as printed (2*(148)*48000=14.208M); it only reproduces 14.064M under the operation order (2·145+3)·48000=293·48000=14.064M. This is very likely a rendering/typesetting artifact in the source PDF's formula rather than an error in the final published number, since the final number (14.064) is stated unambiguously and repeated in Table 1.
  CORRECTION: Minor: the claim's parenthetical '(296 ops/sample)' is not quite consistent with the paper's own final MFLOPS figure — 296*48000=14.208 MFLOPS, not 14.064. The paper's Table-1 value of 14.064 MFLOPS corresponds to 293 ops/sample (i.e., 2·145+3, not 2·(145+3)), most likely reflecting a formula-typesetting quirk in the original paper. The final MFLOPS figures cited (6.96, 14.064, 14.592, 196.608) are all independently confirmed correct.

[confirmed] 10. In a 32,768-sample BRIR (44.1kHz, 0.65s RT60, SBSBRIR x0y0) listener-rotation experiment, only TVOLAP (2L=1024/M=32) gives artifact-free immediate position switching vs. OLA/OLS clicks and WOLA's too-slow crossfade.
  evidence: Verified verbatim in my own direct extraction of the arXiv PDF: 'a room (reverberation time approx. 0.65 s) was rendered... The listener rotated counterclockwise in steps of 45 degrees... using BRIR set 'x0y0' from the Salford-BBC Spatially-sampled Binaural Room Impulse Responses (SBSBRIR)... NIR = 32,768 samples at fs=44.1kHz... TVOLAP used a block size of L2=1024 and M=32 partitions... OLA and OLS produce perceptible click sounds, while WOLA performs a smooth cross-fade... but takes too much time... TVOLAP is closest to the intended scene.' Matches the claim's parameters and conclusion exactly.

[confirmed] 11. TVOLAP has constant computational load at IR-switch time (no spike), unlike CF-TDC which runs two full convolvers in parallel plus a small fixed mixing overhead (3·fs extra ops/sec ≈ 0.144 extra MFLOPS at 48kHz) for the crossfade duration.
  evidence: Verified verbatim in my own direct extraction of the arXiv PDF: 'multiplications and one addition per audio sample, resulting in 3·fs = 0.144 extra MFLOPS' (describing CF-TDC's fixed mixing overhead) and 'TVOLAP does not introduce peaks in the computational load when changing the impulse response, as only the transfer function partitions are exchanged.' Both figures/statements match the claim exactly.

